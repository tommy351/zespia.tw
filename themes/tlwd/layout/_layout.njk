<!DOCTYPE html>
<html lang="zh-TW">
<head>
  {{ partial('_partial/head') | safe }}
</head>
<body class="bg-background text-content antialiased scrollbar-dark">
  <div class="container max-w-3xl mx-auto px-4 flex flex-col min-h-screen">
    <div class="flex-grow">
      {{ partial('_partial/header') | safe }}
      {{ body | safe }}
    </div>
    <div class="flex-shrink-0">
      {{ partial('_partial/footer') | safe }}
    </div>
  </div>
</body>
</html>
